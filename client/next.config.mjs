/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["localhost", "avatars.githubusercontent.com", "github.com","robohash.org"],
  },
};

export default nextConfig;
